{
  "name": "@shorter/eslint-config",
  "type": "module",
  "private": true,
  "files": [
    "base.mjs",
    "nuxt.mjs",
    "vue.mjs"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "clean:modules": "rm -rf .turbo node_modules",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "peerDependencies": {
    "eslint": "^9.15.0"
  },
  "dependencies": {
    "@antfu/eslint-config": "^3.9.1"
  },
  "devDependencies": {
    "eslint": "^9.15.0"
  }
}
